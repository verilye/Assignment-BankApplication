@model HomeViewDTO
@{
    ViewData["Title"] = "Home Page";
}

<div>
    <div id="homePageLeft">
        @await Component.InvokeAsync("SideBar")
    </div>
    <div class="col-5 mt-4" id="homePageMiddle">
        <div class="row" id="sidebar-viewport">
            <div id="depositView" class="d-block">@await Component.InvokeAsync("Deposit")</div>
            <div id="withdrawView" class="d-none"> @await Component.InvokeAsync("Withdraw")</div>
            <div id="transferView" class="d-none">@await Component.InvokeAsync("Transfer")</div>
            <div id="myStatementsView" class="d-none"> @await Component.InvokeAsync("MyStatements")</div>
            <div id="myProfileView" class="d-none">@await Component.InvokeAsync("MyProfile")</div>
            <div id="billPayView" class="d-none">@await Component.InvokeAsync("BillPay")</div>
        </div>
        <div class="row" id="account-display">
            <div class="rounded" style="width:100%">
                <div class="bg-primary rounded">
                    <h4 class="p-3 text-white">Accounts:</h3>
                </div>
                @foreach (var item in Model.AccountViewModels)
                {
                    <div class="card p-3 container-fluid" style="width:100%">
                        <div class="row">
                            <div class="col-3">Account Number: @item.Account.AccountNumber</div>
                            <div class="col-2">Balance: $@item.Balance</div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
    <div class="col-2" id="homePageRight">
        @await Component.InvokeAsync("ProfileDisplay")
    </div>
</div>


<style>
    #homePageLeft {
        margin-left: 0vw;
        position: absolute;
    }

    #homePageMiddle {
        margin-left: 20vw;
        width: 50%;
        position: absolute;
        height: 70vh;
    }

    #account-display {
        margin-top: 75vh;
        margin-left: 20vw;
        position: absolute;
        height: 30vh;
        width: 50%;
    }

    #homePageRight {
        position: absolute;
        margin-left: 83vw;
    }
</style>